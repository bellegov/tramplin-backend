spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/tramplin 
    username: root
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080

minio:
  url: http://localhost:9000
  internal-url: http://minio:9000
  access-key: root
  secret-key: rootpassword
  bucket-name: tramplin-images


initial-admin:
  email: admin@tramplin.ru
  password: 12345678
  display-name: Главный Администратор